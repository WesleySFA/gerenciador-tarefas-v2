# Railway (auto-injected when MySQL service is linked)
# MYSQL_URL=mysql://user:password@host:port/database

# Local development
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=sua_senha
DB_NAME=tarefas_db
JWT_SECRET=sua-chave-secreta-aqui
PORT=3000
